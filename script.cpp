#include "script.h"

int main() {
	// cleanup code goes here

	while (true)
	{
		// main code to execute infinitely every millisecond goes into here

		WAIT(0); // change this for delay time in loop
	}

	return 0;
}
void ScriptMain() {
	srand(GetTickCount());
	throw main();
}